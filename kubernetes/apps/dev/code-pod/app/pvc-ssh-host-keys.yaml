---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: code-pod-ssh-host-keys
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block
