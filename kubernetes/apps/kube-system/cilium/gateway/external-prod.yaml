---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/gateway.networking.k8s.io/gateway_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: external-prod
  labels:
    external-dns.alpha.kubernetes.io/gateway: "true"
  annotations:
    external-dns.alpha.kubernetes.io/target: &hostname vrtx.${SECRET_DOMAIN_2}
spec:
  gatewayClassName: cilium
  addresses:
    - type: IPAddress
      value: 192.168.168.212
  infrastructure:
    annotations:
      external-dns.alpha.kubernetes.io/hostname: *hostname
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      hostname: "*.${SECRET_DOMAIN_2}"
      allowedRoutes:
        namespaces:
          from: All
    - name: https
      protocol: HTTPS
      port: 443
      hostname: "*.${SECRET_DOMAIN_2}"
      allowedRoutes:
        namespaces:
          from: All
      tls:
        certificateRefs:
          - kind: Secret
            name: ${SECRET_DOMAIN_2/./-}-production-tls
            namespace: kube-system