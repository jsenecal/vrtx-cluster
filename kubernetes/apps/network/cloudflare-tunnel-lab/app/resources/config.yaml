---
originRequest:
  originServerName: vrtx.${SECRET_DOMAIN}

ingress:
  - hostname: ${SECRET_DOMAIN}
    service: &svc https://cilium-gateway-external-lab.kube-system.svc.cluster.local
  - hostname: "*.${SECRET_DOMAIN}"
    service: *svc
  - hostname: ${SECRET_DOMAIN_3}
    service: *svc
  - hostname: "*.${SECRET_DOMAIN_3}"
    service: *svc
  - service: http_status:404
